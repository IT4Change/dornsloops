# Absolute base URL of the deployment — no trailing slash.
# Required for link previews: Open Graph needs absolute URLs for the preview
# image and video. Read at build time, so re-generate after changing it.
NUXT_PUBLIC_SITE_URL=https://loops.example.org
